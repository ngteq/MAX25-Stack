# Legacy configuration (deprecated)

Use [../baycom-pr.ini](../baycom-pr.ini) and [../modems.ini](../modems.ini) instead.

These `.env` files remain for backward compatibility with `baycom-pr-ctl -f dual` and installs to `/etc/baycom/legacy/pccom.env*`.
